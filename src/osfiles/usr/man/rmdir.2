.TH rmdir(2),System Calls Manual,rmdir(2)
.SH ADDRESS
$9D72
.SH DESCRIPTION
- Deletes an empty directory whose name is pointed to by .AX

- Fails on an non-empty directory
.SH RETURN VALUES
- Returns 0 on success, non-zero on failure

