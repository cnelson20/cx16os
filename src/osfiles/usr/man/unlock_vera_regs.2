.TH unlock_vera_regs(2),System Calls Manual,unlock_vera_regs(2)
.SH ADDRESS
$9D96
.SH DESCRIPTION
Releases the calling process' lock on VERA, if it has the lock
.SH ARGUMENTS
- None
.SH RETURN VALUES
- .A = 0 on success (if the process had the lock), non-zero on failure

