.TH send_byte_chrout_hook(2),System Calls Manual,send_byte_chrout_hook(2)
.SH ADDRESS
$9D87
.SH DESCRIPTION
Send a byte directly to the chrout hook. Fails if hook is not in use.
.SH ARGUMENTS
- .A holds the byte to send
.SH RETURN VALUES
- .A is preserved

- .X = 0 on success, non-zero on failure

